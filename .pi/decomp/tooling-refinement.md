# Decomp Tooling Refinement Log

Append-only notes from decomp chunks. Use this to audit which tooling changes were suggested by real chunk usage rather than fresh-context intuition.

## Chunk 378 — 2026-06-10T13:17:20Z
- Matched: no
- Summary: Family/similarity routing quickly found high-leverage siblings (`func_1502B110`, `func_1502B9B4`, `func_1000FC18`) and skeleton seeds got close, but no full ROM match landed.
- Likely cause / blocker: Candidates were blocked by IDO edge cases rather than high-level algorithm misunderstanding: stack scratch-slot placement/trailing padding for `func_1502B9B4`, and scheduling/global-reload ordering for `func_1000FC18`.
- Tooling observations: Family routing and skeleton enrichment are useful; read-only queue behavior mostly worked, but family mode surfaced already-matched `func_1502B6BC` due stale pending queue state. `decomp_permute` was ineffective on a strong near-miss because it reported `compiled=0, forks=0` instead of exploring mutations.
- Improvement opportunities: Make candidate selection live-filter pending entries whose GLOBAL_ASM pragma no longer exists; make `decomp_permute` auto-retry without `--isolate` when an isolated run produces zero compiled mutations; require/record per-chunk tooling observations in-tree so future tooling sessions can audit recommendations against actual chunk data.
## Chunk 379 — 2026-06-10T13:40:05.049Z
- Matched: no
- Summary: No full match this chunk. Family/similarity routing produced several high-leverage candidates: func_1502B9B4 matched through epilogue but missed only two target trailing padding nops; func_1000FC18 stayed at a 0-instruction scheduling near-miss around post-call D_80042760 reload ordering; func_151347CC reached a 1-instruction near-miss where only a branch-likely operand order differs (`bnel t9,v1` vs `bnel v1,t9`).
- Likely cause / blocker: No algorithmic blockers on the best candidate; remaining misses are very low-level IDO codegen/padding issues. The strongest live blocker is Transmuter: even no-isolate runs on non-varargs and one-instruction near-misses report iters=0, compiled=0, forks=0, so the tool is not exploring mutations when it is most needed.
- Tooling observations: Family/similarity routing and skeleton context were effective: they surfaced close siblings and enabled rapid near-misses. The new stale-pragma filtering worked because already-decompiled func_1502B6BC was not served. Transmuter diagnostics are clearer after the update, but the backend still compiles zero mutations for func_1502B110, func_1000FC18, and func_151347CC, including explicit isolate=false runs.
- Improvement opportunities: Investigate Transmuter CLI invocation/report parsing because runs terminate with iters=0/compiled=0/forks=0 even on simple valid C seeds; add a focused branch-operand-order mutation/search mode for one-instruction `bne/beq` operand swaps; teach decomp tooling to detect/handle trailing target padding nops separately from function body diffs so near-exact C like func_1502B9B4 is not treated as a normal structural failure.
## Chunk 380 — 2026-06-10T13:44:47.556Z
- Matched: yes
- Summary: Matched and accepted `func_1507839C` in `game_A28B0.c`, raising progress to 180/934. Also explored `func_1001F79C`: compact typed Gfx rewrite achieved the correct 0x38 frame but remained a delay-slot/branch near-miss around storing the second `osVirtualToPhysical` result.
- Likely cause / blocker: Success came from correcting the commented reconstruction's comparison polarity and then replacing `D_800CC2E8[idx]` with raw byte-stride addressing to preserve IDO register allocation.
- Tooling observations: Similarity routing was effective: the queue surfaced a commented JUSTREG candidate with strong same-file references. `decomp_attempt` diff was precise enough to identify the wrong symbol first (`D_800CC2D0` vs `D_800CC2E8`) and then the register-shifting caused by array indexing. `decomp_diff` gave no useful details for a length-mismatch hard function (`func_1001F79C`), so manual raw ASM comparison was required.
- Improvement opportunities: Add a heuristic to suggest byte-addressed pointer arithmetic when a global array access differs by a final shift-temp (`sll newTemp, oldTemp, elementShift`) and downstream register drift. Improve `decomp_diff` for length mismatches by still showing an aligned raw-instruction diff around the first scheduling/length divergence, especially branch-delay filled stores vs explicit store+nop.
## Chunk 381 — 2026-06-10T14:00:08.691Z
- Matched: yes
- Summary: Matched and accepted `func_1513418C` in `game_161520.c`. Explored `func_15077F64`, `func_1001F5A4`, and `func_1507DFE4` but rotated off due permuter/tooling limitations or prototype/layout blockers.
- Likely cause / blocker: `func_1513418C` succeeded by simplifying locals to get the target 0x28 frame, reordering the final float stores to fix FPR allocation, and using an accumulator local (`temp_a2 = temp_v1; temp_a2 += offset;`) to fix the last `addu` operand order.
- Tooling observations: `decomp_attempt` diffs were very helpful on `func_1513418C`, narrowing it to a single `addu` operand-order mismatch. `decomp_permute` repeatedly returned 0 compiled mutations for several valid near-miss seeds (`func_15077F64`, `func_1001F5A4`, `func_1507DFE4`, `func_1513418C`), so manual iteration was necessary. Direct edits to source prototypes are blocked, which prevented testing a likely TU-level prototype correction for `func_1507DFE4`.
- Improvement opportunities: Improve Transmuter fallback for raw-pointer-heavy C so it can mutate seeds that currently report 0 compiled attempts; allow a safe, temporary prototype-override mechanism in `decomp_attempt` for testing file-level prototype hypotheses without manual edits; add an automatic suggestion for accumulator locals when the only diff is commutative `addu` operand order in a call argument.
## Chunk 382 — 2026-06-10T14:05:42.036Z
- Matched: yes
- Summary: Matched and accepted `func_150151D4` in `game_40490.c`. Also attempted `func_1504ADD0` in `game_77AD0.c` but skipped after register-allocation/global-hoist attempts and a Transmuter run with 0 compiled mutations.
- Likely cause / blocker: `func_150151D4` succeeded by using direct typed casts on a stack payload array to avoid temporary stack-pointer registers, byte-storing `arg0+0x14` instead of struct `u16` assignment, and reordering independent word-zero payload stores. `func_1504ADD0` is blocked on saved-register allocation and global-address hoisting (`D_800CC284`, stack buffer pointer, `D_800D154C`) rather than high-level control flow.
- Tooling observations: `decomp_attempt` was effective for `func_150151D4`, narrowing the final mismatch to two swapped zero stores. For `func_1504ADD0`, non-match output often reported length mismatch without detailed alignment despite generated ASM being close enough to inspect manually; `decomp_permute` again returned 0 successful compiled mutations on a valid near-miss seed.
- Improvement opportunities: Add a focused diff mode for loop setup/register-hoist mismatches that maps saved registers to locals/globals across target vs generated. Improve Transmuter isolation for functions with globals and stack arrays so it can compile mutations instead of immediately reporting 0 compiled attempts. Add automatic hints for direct typed casts on stack arrays when `(s32)local` casts introduce temporary stack-pointer registers.
## Chunk 383 — 2026-06-10T14:11:46.102Z
- Matched: yes
- Summary: Accepted `func_15077F64` in `game_A28B0.c` after `decomp_diff`/`decomp_inspect` revealed the current generated assembly was already an exact match. Attempted `func_15160A58` in `game_18D770.c`; got a 0-instruction-count near-miss using a `struct17` block assignment for the 3-word payload copy, then skipped after scheduling/register issues and Transmuter failures.
- Likely cause / blocker: `func_15077F64` succeeded because the source/object already matched exactly with the call-surviving u8 spilled at `sp+0x1E`. `func_15160A58` is blocked on early stack-field scheduling: IDO hoists the `arg1` byte load/store before the target's header stores, and adding explicit temps inflates the frame from 0x68 to 0x70.
- Tooling observations: `decomp_diff` unexpectedly reported score 1 for `func_15077F64` even though the queue still listed it pending, and `decomp_inspect` confirmed a perfect match; this was useful for recovering an already-solved near-miss. For `func_15160A58`, Transmuter returned 0 compiled mutations both with isolation enabled and disabled on a valid 0-instruction-off seed.
- Improvement opportunities: Queue refresh should detect functions whose current compiled object already matches before serving them as pending, or surface a 'verify/accept now' action. Transmuter needs better support for function signatures with many stack arguments and struct assignment seeds. A schedule-focused suggestion system for stack payload wrappers could recommend assignment ordering or local layout changes when the only issue is loads/stores around header/data construction.
## Chunk 384 — 2026-06-10T14:21:12.606Z
- Matched: no
- Summary: Worked several candidates but did not match a function this chunk. `func_151670C0` reached a 0-instruction-count near-miss for the nested table/list walker; `func_1000A348` and `func_150C2700` were skipped due 8+ prior attempts; `func_151415D4` remained flat at ~0.493 despite a fresh pointer-recompute/FPR attempt.
- Likely cause / blocker: Main blockers were register/scheduling in near-miss loops: `func_151670C0` needs `$s7` for `D_800DCE50`, `$fp` for loop limit, and `lw t9,0(s2); jalr t9` with the next-pointer store in the delay slot, while viable C shapes either swap `$s7/$s8` or use `$v0` for the callback. `func_151415D4` is blocked on FPR allocation/order in the piecewise interpolation and wrap loop.
- Tooling observations: `decomp_queue next` served candidates with 8+ prior attempts despite the prompt stating these should be auto-rotated; both `func_1000A348` and `func_150C2700` had explicit 'DO NOT REPEAT' histories and were manually skipped. `decomp_permute` returned 0 successful compiled mutations for both `func_151670C0` and `func_151415D4`, including an isolate=false retry for `func_151670C0`, even though the seeds compiled via `decomp_attempt`.
- Improvement opportunities: Improve queue filtering to automatically exclude or deprioritize 8+ attempt functions unless `nearMiss` is explicitly requested. Transmuter should handle valid decomp_attempt seeds with many locals/loops instead of producing 0 mutations, and it would help to have targeted mutations for callback-call register shaping (`jalr t9` vs `jalr v0`) and saved-register allocation swaps (`$s7/$s8` base vs loop-limit locals).
## Chunk 385 — 2026-06-10T14:32:46.902Z
- Matched: no
- Summary: No function matched this chunk, but made concrete progress on two fresh candidates. `func_15007440` was reconstructed semantically and reached a 3-instruction near-miss after switching fixed byte loops to label/goto form to prevent IDO unrolling. `func_1000E2F4` reached a 0-instruction-count near-miss after using typed table indexing plus `*(s32 *)&D_8002B074[idx].unk4` for word loads and passing `i` directly to the `u8`-prototyped `func_100084D8` to put `andi a0,s1,0xff` in the call delay slot.
- Likely cause / blocker: Remaining blockers are saved-register allocation and branch-shape minutiae. `func_15007440` needs checksum locals in `$v1/$v0/$a0`, direct saved-byte `lb/sb` through `$a2`, and the fill loop as `bne` backedge plus post-fill branch while still avoiding IDO's 16-byte unroll. `func_1000E2F4` is semantically and instruction-count close but saved registers are rotated (`arg0` in `$s4`/`$s5` instead of target `$s6`, `-1`/limit in wrong saved regs) and a few temp regs differ.
- Tooling observations: `decomp_permute` repeatedly returned 0 successful compiled mutations for valid, compiling near-miss seeds (`func_15007440` at 3-instruction off and `func_1000E2F4` at 0-instruction off), including isolate=false retries. For `func_1000E2F4`, the permuter also initially failed to find attempt history despite multiple `decomp_attempt` calls, requiring explicit seed input.
- Improvement opportunities: Add targeted Transmuter rules for saved-register rotation via declaration/assignment reordering, and for converting `if (ptr != end) goto loop` shapes between `beq exit; b loop` and `bne loop; b after`. Improve Transmuter parsing of goto-label old-C seeds so it can mutate these common decomp near-misses instead of compiling 0 variants. Queue/tooling could surface a concise register-allocation map (local -> saved reg) for 0-instruction near-misses to guide declaration-order changes.
## Chunk 386 — 2026-06-10T14:43:09.144Z
- Matched: no
- Summary: No match accepted in chunk 386. Continued `func_1000E2F4` briefly (still a 0-instruction-count near-miss blocked by saved-register allocation), reconstructed `func_15075938` to a strong near-miss using the in-source commented scalar typing (`u8 temp_v0`, `u8 changed`, `s32 temp_a0`, `s32 phi_v1`) which matches the whole second half and gets actor/delta in `$v1/$a0`, and tried `func_1513E6E8` as a raw-offset float integrator with volatile flag/global-float loads.
- Likely cause / blocker: All progress plateaued on IDO codegen minutiae rather than semantics. `func_1000E2F4` still needs arg0 in `$s6` while keeping `-1`/limit in `$s5/$s4`; `func_15075938` needs the no-table-local scalar version's register allocation plus the persistent-table version's `bgezl` delay-slot shape and exact early `%hi` hoists; `func_1513E6E8` needs raw base pointer `$v0`, global float pointer `$a1`, target operand order for `mul.s/add.s`, and exact flag reload scheduling.
- Tooling observations: `decomp_permute` again produced 0 compiled mutations for a valid near-miss seed on `func_15075938`, even after automatic no-isolate retry. `decomp_diff` returned only a score for `func_15075938` register focus, which was not actionable for a near-miss where the generated assembly clearly showed register/scheduling differences.
- Improvement opportunities: Improve Transmuter support for old-C decomp seeds with casts like `(*(u8 **)&GLOBAL)[idx]` and nested if ladders so it can mutate them instead of compiling 0 variants. Add a register allocation/scheduling diagnostic that compares target vs generated live local roles (e.g. actor ptr, delta, changed flag, table pointer) and suggests declaration/type changes. Add a branch-likely shaping rule for `if (x < 0) ... else ...` where one version emits `bgez` with nop and a semantically close variant emits `bgezl` with the else-path load in the delay slot.
## Chunk 387 — 2026-06-10T14:51:14.745Z
- Matched: no
- Summary: No function matched in chunk 387. Per strategy adjustment, switched tiers/regions: tried debugger `func_16001700`, init `func_1000E46C`, and near-miss game `func_15077F64` instead of continuing the prior hard family. `func_16001700` semantics were reconstructed as a controller/PIF DMA setup with two wait loops and a preincrement `0xFF` fill; `func_1000E46C` semantics were reconstructed as a clamped percent/bitmask updater; `func_15077F64` got a new frame-shaping seed where an unused `volatile u64 framepad` successfully forced the target 0x28-byte frame and stack byte at `sp+0x1e`.
- Likely cause / blocker: All candidates blocked on IDO register allocation/scheduling rather than semantics. `func_16001700` keeps spilling the current-count temp to stack instead of using `$s0` while target uses `$s0/$s1`; `func_1000E46C` similarly spills arg1/arg2 locals instead of saving them in `$s1/$s0`, and IDO rejects fixed-register syntax; `func_15077F64` has known strong 0.824 seeds but needs both the target 0x28 frame and exact global-load/register ordering for `$t0/$a3/$t5/$t6`.
- Tooling observations: `decomp_diff` returned only the score for both stack/register focused requests, providing no useful guidance despite clear saved-register vs stack-spill differences. `decomp_permute` again compiled 0 mutations for valid old-C seeds (`func_1000E46C` and `func_15077F64`), so plateau guidance could not actually help. The queue served `func_15077F64` via nearMiss despite 10 prior attempts, which was useful for revisiting but should perhaps surface the historical best generated assembly directly.
- Improvement opportunities: Add a saved-register allocation diagnostic: identify when target saves `$s0/$s1` for locals but generated spills those roles to stack, and suggest concrete C patterns known to force callee-saved allocation. Improve Transmuter parsing/mutation for old-C declarations, raw casts, local padding arrays, and volatile framepad seeds so near-misses produce actual compiled variants. Add a queue option to retrieve the best prior attempt’s generated ASM and diff without re-running, since long prior-attempt lists omit generated assembly for the most relevant best seed.
## Chunk 388 — 2026-06-10T15:02:03.071Z
- Matched: no
- Summary: No match in chunk 388, but switched strategy as requested to a different family/difficulty route. Tried family-routed leaf float helper `func_1513E6E8` and then unfiltered stack-builder `func_151623F4`. For `func_151623F4`, reconstructed full semantics and got a correct 0x50 frame/stack offsets using a `s32 sp30[8]` payload array plus negative-offset header stores; this is a 2-instruction near-miss seed.
- Likely cause / blocker: `func_1513E6E8` blocked on IDO folding the `arg0+0x110` base into direct offsets and assigning the flag word to `$v0`; volatile variants fixed flag reloads but reversed float operand/add ordering. `func_151623F4` is semantically correct and frame-correct, but schedule/register allocation differs in the table-copy/header setup; splitting locals worsened layout, so the single-array negative-offset seed is best.
- Tooling observations: `decomp_diff` again returned only score text for clear near-misses, with no side-by-side details, so it could not guide the 2- or 3-instruction deltas. `decomp_permute` compiled 0 mutations for three valid seeds, including simple leaf/stack-builder functions and with isolate=false in one case; plateau guidance is therefore ineffective on these old-C/raw-cast seeds.
- Improvement opportunities: Improve `decomp_diff` to show actual target/generated aligned instructions even when the score is low due address/register shifts. Add Transmuter support for old-C raw pointer casts, negative stack-offset expressions, and volatile local seeds so it can compile mutations for near-misses like `func_151623F4`. Add a pattern/rule for forcing IDO to materialize a base pointer (`addiu v0,a0,imm`) instead of direct-offset folding in leaf float-update functions.
## Chunk 379 — 2026-06-10T15:48:16.803Z
- Matched: yes
- Summary: Matched and accepted `func_1502B9B4` in `game_57FA0.c` as a variadic asset-offset helper with stack-aligned read buffer and post-loop encoded-length handling. Also explored `func_1502B110` extensively; its C shape is close but still blocked by stack-slot/scheduling differences around the vararg status local and initial `$s2` setup.
- Likely cause / blocker: Success came from recognizing that `func_1502B9B4`'s generated C was instruction-identical except for terminal nop padding in the target asm; full ROM SHA passed after acceptance. `func_1502B110` remains blocked on precise IDO stack local placement and prologue scheduling.
- Tooling observations: `decomp_attempt` showed raw generated ASM for `func_1502B9B4` was an exact functional match but reported a length mismatch due only to two target trailing nops; full SHA acceptance proved this padding can be ignored/modelled separately. `decomp_permute` was attempted on both plateaued functions but compiled 0 mutations, reporting an unhealthy smoke test, so it could not assist.
- Improvement opportunities: Improve diff/match tooling to classify/ignore terminal alignment nop padding (or synthesize/linker-padding expectations) when the full functional body matches through `jr` delay slot. Investigate Transmuter 0-compile failures for old-C variadic functions and float functions; it currently provides no search coverage on these plateau cases.
## Chunk 381 — 2026-06-10T15:58:37.600Z
- Matched: yes
- Summary: Matched and accepted `func_1000FC18` in `init_EB00.c`, a multi-field sound table matcher/flag updater. Also brought `func_151347CC` to a one-instruction near-match (only `bnel` operand order in the `0x2D` replacement case differs) and explored `func_151415D4` briefly but skipped due persistent float FPR/register scheduling plateau.
- Likely cause / blocker: `func_1000FC18` matched by using a hybrid loop style: `ptr = &D_80041FE0[i]` for comparisons but indexed `D_80041FE0[i]` for the call argument and final store, which fixed IDO scheduling around the post-call global count reload. `func_151347CC` blocker is a commutative branch operand-order issue (`bnel t9, v1` vs `bnel v1, t9`) that ordinary C expression flipping did not affect.
- Tooling observations: `decomp_attempt` was very effective for identifying the exact hybrid pointer/indexed access pattern in `func_1000FC18`. `decomp_permute` again reported an unhealthy/0-successful-compile diagnostic for near-misses, even while raw output showed some compiled iterations, and did not provide a candidate for the one-instruction branch operand issue.
- Improvement opportunities: Add a targeted permuter rule for branch compare operand shaping (`bnel regA, regB` vs `bnel regB, regA`) using condition polarity, temporary extraction, and goto/else variants. Fix Transmuter health reporting so compiled iteration counts are consistent and candidate code is surfaced for 1-instruction near-misses.
## Chunk 385 — 2026-06-10T17:33:45.343Z
- Matched: no
- Summary: Worked primarily on func_1502B110 and reconstructed its likely behavior: optional base offset defaulting to D_AB1950, varargs loop over arg4+ while arg3 >= 2 calling func_1502AC88 with a state word, then final func_1502AF04(offset,arg2,next,arg1) and return. Best normal attempts were instruction-count matches but not exact: IDO placed the state/return stack locals at 0x38/0x34 instead of target 0x3C/0x38 and scheduled the s2 save/move differently. Also revisited near-miss func_15077F64 with Transmuter, which improved its isolation score but did not verify in the full TU.
- Likely cause / blocker: Blocked by stack-slot shaping/context-sensitive IDO scheduling. For func_1502B110, scalar/struct locals produce the right control flow but wrong local offsets; OOB/derived-pointer tricks can force 0x3C/0x38 but trigger alias/UB optimizations that collapse the offset/state variable and change the function structurally.
- Tooling observations: decomp_attempt was useful for exposing exact generated ASM and showing that the candidate is structurally correct despite low-ish scores dominated by prologue/local-offset drift. decomp_permute improved both functions but did not return the best mutated C body in the visible output, making it hard to continue from its 11-instruction/18-instruction improved states.
- Improvement opportunities: Have decomp_permute always print or save the best full-TU candidate C body (even on non-match), plus a concise local-stack map (variable/expression -> stack offset) for each attempt. A targeted stack-slot shaper/mutation for scalar-to-struct/array fields without UB would likely unlock func_1502B110.
## Chunk 386 — 2026-06-10T17:41:05.873Z
- Matched: no
- Summary: Worked three candidates this chunk. func_151347CC was reconstructed almost completely from the high-similarity func_151419D0/func_1513555C references: cases 0/3 and 0x11 call func_1516972C on matching id/byte, case 0x16 compares arg1 to arg0+0x1C, and case 0x2D swaps the stored pair; best attempt is a single instruction off, only `bnel v1,t9` vs target `bnel t9,v1`. Also tried func_151415D4 (piecewise float ramp plus wrap) and func_1507DFE4 (input probe + angle/sound side effects), with partial but non-matching progress.
- Likely cause / blocker: func_151347CC is blocked by branch-likely operand order for an equality check; expression polarity and local declarations either keep the right registers with reversed operands or fix the branch operand order while spilling the first loaded value to a2 instead of v0. func_1507DFE4 is blocked by prototype/stack-layout shaping: existing `func_15084D70(s32,u8,...)` forces byte reloads while target passes original a0 as a1, and function-pointer cast fixes that but emits jalr/frame drift.
- Tooling observations: decomp_queue's similarity routing was excellent: func_151347CC reached 0.9863 on the first attempt from the matched reference. decomp_permute identified the same one-instruction plateau but did not reveal its best candidate source, so it could not help bridge the operand-order issue. decomp_attempt's generated ASM made prototype problems in func_1507DFE4 very clear (`lbu a1` from saved arg vs target `move a1,a0`).
- Improvement opportunities: Add a targeted branch-operand-order mutation/search for commutative `==/!=` branch-likely conditions that preserves register allocation but swaps rs/rt in emitted bne/beq, or a pattern note for how to force IDO from `bnel v1,t9` to `bnel t9,v1`. Also allow per-attempt local prototype shadowing or safe call-site prototype overrides without jalr so functions like func_1507DFE4 can pass a full-width value despite an in-file prototype declaring `u8`.
## Chunk 391 — 2026-06-10T19:17:48.889Z
- Matched: yes
- Summary: Matched func_15075938 (game_A28B0.c, 70 instr) — a nearest-frame selection helper. Key: redeclared D_800D2108 from u8[] to u8* via contextPatch (it's loaded with lw, i.e. a pointer global), then used byte-addressed pointer arithmetic *(u8*)((s32)D_800D2108 + (s32)field) in the temp_a0<0 branch to force base-first operand evaluation, fixing a t8/t9 register swap. Progress 179→180.
- Likely cause / blocker: Matched via contextPatch type change + byte-addressed pointer arithmetic.
- Tooling observations: contextPatch worked perfectly for the type redeclaration (u8[] -> u8*) in variables.h and auto-reverted on every non-match, committing cleanly only via decomp_accept's SHA gate. decomp_attempt's full raw ASM + structural diff made the 0.9571 (0-instr-off) register-swap diagnosable. decomp_permute could NOT fix the operand-evaluation-order register swap (plateaued at 15 across 10k compiles, depth 2) — this was an LLM-level expression-shape fix, not a mutation. Similarity ranker (tools/decomp_similarity.py --rank-pending) returned several already-matched functions (func_1502B6BC, func_1502B8E0, func_1502B9B4) as 'pending' top candidates — stale data wasted ~4 tool calls verifying they were already decompiled.
- Improvement opportunities: 1) decomp_similarity.py --rank-pending should cross-check against actual GLOBAL_ASM pragmas present in source (or the queue's matched set) to avoid surfacing already-decompiled functions as pending. 2) For 0-instruction-off near-misses that are pure operand-evaluation-order register swaps, the permuter has no rule that reorders subexpression evaluation (base vs index of array access) — adding an 'array-index-to-ptr-arith' rewrite rule would let it brute-force these. 3) Functions like func_15007440 with statically-bounded byte-copy loops get unrolled by IDO's memcpy optimizer (148 vs 70 instr) — a queue hint flagging 'likely-loop-unroll' would save attempts.

## Chunk 391 follow-up: implemented 3 harness improvements

Acted on the improvement opportunities from chunk 391:

1. **Pointer-global detector (decomp_queue next context).** When a referenced
   global is declared as an array in variables.h but the target asm loads it via
   `lw %lo(sym)` (i.e. it stores a pointer), the candidate context now emits a
   "Pointer-Global Hints (contextPatch candidates)" section telling the model to
   redeclare it as `T *sym;` and index via the pointer / byte-addressed arithmetic.
   This is exactly the D_800D2108 insight that won func_15075938, now surfaced
   automatically instead of inferred from raw asm.

2. **Operand-eval-order detector (decomp_diff).** When the structural diff is a
   pure register-name swap between two adjacent loads where the target loads a
   pointer/base global (`lw %lo`) before the index byte (`lbu`), decomp_diff now
   suggests rewriting as `*(T *)((s32)PTR + (s32)obj->field)` to force base-first
   evaluation — and explicitly notes the permuter cannot do this (it plateaued at
   15 across 10k+ compiles on func_15075938).

3. **Similarity staleness fix (tools/decomp_similarity.py).** --rank-pending now
   cross-checks each pending entry against the live GLOBAL_ASM pragma in source
   (has_live_pragma), dropping entries that were decompiled outside this tool.
   Verified: func_1502B6BC / func_1502B8E0 / func_1502B9B4 (already matched) no
   longer surface; genuinely-pending func_1502B110 still ranks #1.

Still open: queue hint for likely-loop-unroll (memcpy-shaped bounded byte loops
like func_15007440 get unrolled to 148 vs 70 instr); permuter rule for
array-index→ptr-arith rewrites.
## Chunk 392 — 2026-06-10T20:23:46.003Z
- Matched: yes
- Summary: Matched and accepted func_1502B110 (game_57FA0.c). Key was mutating formal arg0 directly as the offset accumulator, which forced target prologue `sw s2; move s2,a0` before other callee-save stores; changing arg3 to u32 emitted the target `sltiu` loop tests. Also worked func_151347CC to a 0-instruction-off near-match (best score 0.9863) with only one remaining branch-likely operand order diff (`bnel t9,v1` target vs `bnel v1,t9` generated).
- Likely cause / blocker: Succeeded on func_1502B110 via direct formal-argument accumulator and unsigned loop count. func_151347CC remains blocked by a one-instruction branch-likely operand order canonicalization that local temp/volatile/goto rewrites did not fix.
- Tooling observations: decomp_queue similarity routing correctly served func_1502B110 after the pending-staleness fix. decomp_attempt made the signedness issue obvious at 0.971 (`slti` vs `sltiu`), enabling a quick match. For func_151347CC, Transmuter plateaued at one instruction off and could not fix branch operand order; adding an explicit temp actually worsened allocation across the whole 0x2D block, showing current mutation rules lack fine-grained branch operand order shaping.
- Improvement opportunities: Add a pattern/detector for `slti` vs `sltiu` near-misses to suggest unsigned parameter/local types immediately. Add a branch-likely operand-order tactic/pattern for equality/inequality comparisons (especially `bnel regA,regB` vs `bnel regB,regA`) and consider a Transmuter mutation that rewrites equality guards with labels/condition inversions while preserving existing temp allocation. Store func_151347CC's best code as a near-miss candidate for future focused work.
## Chunk 393 — 2026-06-10T20:31:14.732Z
- Matched: no
- Summary: No new function accepted in chunk 393. Revisited func_151347CC and confirmed the best form is still 0-instruction-off with only `bnel t9,v1` target vs `bnel v1,t9` generated; tried raw-cast memory-left guard, u32 comparison, and s32* arg1 shape with no improvement. Also explored func_151415D4; Transmuter found no improvement. Made useful progress on func_1507DFE4: a contextPatch changing local prototype `func_15084D70(s32,u8,...)` to `func_15084D70(s32,s32,...)` fixes the prologue and removes the unwanted `lbu a1` reload; remaining diffs are stack-local offsets/layout only.
- Likely cause / blocker: No match because func_151347CC is blocked by branch-likely operand order canonicalization, func_151415D4 by stubborn FPR allocation/branch scheduling, and func_1507DFE4 by a nontrivial stack layout: target wants some locals at no-pad offsets while frame is 0x68 and other locals are shifted higher/lower.
- Tooling observations: decomp_attempt was very effective for func_1507DFE4: it exposed the bad prototype as the source of the prologue mismatch, and contextPatch immediately fixed it. However, queue filtering appears odd: `nearMiss` returned func_15077F64 despite 10 prior attempts even though the prompt says 8+ failed attempts should auto-rotate, and similarity filters reported no candidates while unfiltered still found a 69-instruction family-routed candidate. Transmuter did not help func_151415D4, likely because the solution needs structural float/FPR shaping rather than small AST mutations.
- Improvement opportunities: Add a detector for unwanted stack byte reloads caused by local prototypes with u8 params: if target has `move aN,aM` but generated has `lbu aN, stack(arg)`, suggest contextPatch widening the callee prototype parameter to s32. Add a stack-layout assistant that compares desired local offsets against generated offsets and suggests negative-index/positive-index dummy arrays for individual locals (func_1507DFE4 needs sp50/sp4E/sp44/sp48 at no-pad offsets, sp5C/sp64 +8, and sp38/sp3C -4). Investigate why 8+ attempt auto-rotation/filtering did not prevent func_15077F64 from being served.
## Chunk 394 — 2026-06-10T20:35:51.959Z
- Matched: no
- Summary: Worked on func_15007440 in game_33990.c. A direct structured reconstruction compiled but IDO unrolled the checksum/fill/copy loops badly (149 instr); switching the loops to explicit goto labels defeated unrolling and produced the best near-shape (68 instr vs 70 target). Tried u16 accumulator, scalar initialization reorder, structured checksum tail, and do/while byte loops; these either did not change the key register allocation or reintroduced unrolling.
- Likely cause / blocker: Blocked by IDO loop/register shaping: best goto version keeps checksum accumulator in $a0 and index in $v0, while target wants accumulator in $v0 and index in $a0 with `slti`/`bnez` tail; byte loops also need single `bne` backedges without IDO's constant-size unrolling. Transmuter could not run because isolated preflight lacked/failed global symbol context even when externs were embedded.
- Tooling observations: decomp_attempt clearly showed the major shift from 149 generated instructions to 68 when using goto labels, making the anti-unroll tactic obvious. decomp_diff was less useful here because relocation annotation lines disrupted alignment and inflated the displayed diff window. decomp_permute reported no history for func_15007440 despite several decomp_attempt calls, and its isolated compile could not handle the global-heavy source with absolute/undefined symbols.
- Improvement opportunities: Add a pattern for fixed-count byte loops where IDO unrolls: explicit goto labels can suppress unrolling, but branch shape may need further helper transformations. Improve decomp_diff alignment by ignoring relocation annotation lines before LCS alignment. Improve decomp_permute isolation by automatically carrying externs from decomp_attempt history and undefined_syms_auto declarations, especially for `extern u8 SYMBOL[]` absolute-address globals like D_800E0BE0/D_800E0BFC.
## Chunk 395 — 2026-06-10T20:43:25.381Z
- Matched: no
- Summary: No function matched this chunk. Continued func_15007440 briefly and found a better checksum-loop shape (`temp_v1++` before `if (i < 0x1E) goto loop`) that removes the prior `beq + b` tail and puts the pointer increment in the backedge delay slot; still blocked by accumulator/index register allocation and byte-loop backedge shape. Then skipped it and worked func_1000E2F4: direct slot-pointer reconstruction was close, and the best variant expresses the non-arg path as `if (!(flags & 0x10)) { func_100084D8; reload; } temp_v0->unk30 = -1; func_1000CC54(i);`, which produces the target `bnezl flag,...` with the store in the delay slot.
- Likely cause / blocker: func_1000E2F4 is blocked by saved-register assignment after matching the branch structure: best code has target control flow and same length, but IDO assigns long-lived `arg0/-1/limit` as `$s5/$s6/$s4` (or other rotations) while target wants `$s6/$s5/$s4`. Transmuter improved isolated scores but could not find a full-TU match.
- Tooling observations: decomp_attempt was effective at revealing the exact C shape for the branch-likely common-store idiom in func_1000E2F4. decomp_diff remains hard to read for init functions because relocation annotation lines disrupt alignment and make early prologue diffs look noisier than the real issue. decomp_permute ran successfully on func_1000E2F4 but its result summary did not expose the best mutated C, limiting its usefulness when it improves score but fails full-TU verification.
- Improvement opportunities: Add a pattern for `if (!(flags & mask)) { call; reload; } common_store; common_call;` producing `bnezl flags, join` with `common_store` in the delay slot. Improve Transmuter reporting to print the best mutated source even when full-TU auto-verify fails, so near-miss human follow-up can use its improvements. Add a saved-register shaping assistant for long-lived constants/args (e.g. target `$s6=arg`, `$s5=-1`, `$s4=3`) suggesting local-copy/declaration/use-order transformations.
## Chunk 396 — 2026-06-10T20:50:13.062Z
- Matched: no
- Summary: No function matched. Switched strategy as requested: tried debugger-region candidate func_16001700 and family-routed hard init candidate func_1001F79C, rather than continuing the prior game/init blockers. func_1001F79C's in-source reconstruction is structurally close; changing `sp24/sp28/arg2` to signed fixed target `sra` vs generated `srl`, but pointer-output allocation rewrites either did not affect scheduling or inflated the frame. func_16001700 high-level flow was reconstructed (libultra controller PIF setup with two busy-wait loops and PIF fill), but IDO allocated the wait-loop counter on stack/a2 and saved only `$s0` instead of target `$s0/$s1`.
- Likely cause / blocker: Fresh candidates were not easy wins: func_1001F79C is hard and sensitive to stack-temp scheduling; func_16001700 is blocked by wait-loop saved-register allocation (`count/deadline` should be `$s0/$s1`) and the permuter improved only isolated score. Queue default then re-served func_1000E2F4 from the previous no-progress chunk, so I stopped rather than repeat the known saved-register blocker.
- Tooling observations: decomp_queue filters for low/short init/game/debugger are mostly exhausted; familyMode can route to very large hard functions, and skip/default re-served a just-skipped previous blocker. decomp_permute on func_16001700 improved isolated score from 31 to 12 but did not expose the best mutated C source, making the improvement unactionable. decomp_attempt raw assembly was useful for identifying that signed temps fixed `sra` in func_1001F79C.
- Improvement opportunities: Improve queue rotation so `skip` in the current session prevents immediate default re-serving, especially after strategy-adjustment prompts. Add a candidate filter for 'fresh/unattempted in last N chunks' or 'exclude current file/function family'. Expose Transmuter's best mutated source even when full-TU verification fails. Add a saved-register-loop pattern/tool for busy-wait loops that should force two locals into `$s0/$s1` rather than stack/caller-saved temps.
## Chunk 397 — 2026-06-10T21:03:00.752Z
- Matched: no
- Summary: No match accepted, but switched tiers/regions as required and made real progress on a fresh medium init candidate. Near-miss Transmuter on func_15077F64 improved isolated score but no full-TU match; hard init display-list sibling func_1001F5A4 was structurally close but Transmuter failed with 0 compiled mutations. The productive work was func_1000E2F4: reconstructed the loop and reduced it to a 0-instruction-count near-match with correct control flow, branch-likely structure, word table loads, and single delayed `unk30=-1` store.
- Likely cause / blocker: func_1000E2F4 is blocked by saved-register allocation only: generated variants want `arg0` in `$s5` or `$s4` and `-1` in `$s6`, while target wants `arg0` in `$s6`, `-1` in `$s5`, `limit=3` in `$s4`, `slot` in `$s2`, `table` in `$s3`, and `i` in `$s1`. Transmuter improved the isolated score (24→9) but did not find a full-TU match or expose mutated source.
- Tooling observations: decomp_queue low/medium-low filters are exhausted, and promotion of one-function segments 0x43880/0x49be0 failed with unhelpful 'make nothing to be done' output. decomp_attempt was effective for func_1000E2F4 and raw ASM made exact saved-register role differences visible. decomp_permute continues to be partially useful for scoring but not actionable when full-TU fails because it does not provide best C source.
- Improvement opportunities: Add a saved-register role shaper/permuter mode that mutates local declaration/assignment order specifically to target desired `$sN` allocations. Expose Transmuter best mutated C even on full-TU failure. Improve promote diagnostics to show which promoted file/function broke and actual compiler/linker errors. Add queue filters for 'exclude function families tried in previous N chunks' and 'medium only, non-hard, non-nearmiss' to avoid repeatedly returning stale blockers.
## Chunk 398 — 2026-06-10T21:10:49.214Z
- Matched: yes
- Summary: Matched and accepted func_1507DFE4 in game_A9D90.c; progress is now 180/934. Switched away from stalled init/debugger families to a similarity-routed medium game candidate, reconstructed it from same-file sibling func_1507E114, patched the local func_15084D70 prototype to use s32 for the second arg, and fixed stack layout with sp50[3] plus low-slot declaration/order swapping.
- Likely cause / blocker: Success came from identifying that `sp50` was a 3-word outparam block, not a scalar, and avoiding raw negative-index/pointer arithmetic that caused IDO to inflate the frame and spill floats. The final two-instruction near-miss was solved by declaration-order slot-role inversion rather than padding.
- Tooling observations: decomp_attempt with contextPatch was essential because the file-level `func_15084D70` prototype had `u8` for arg1 and generated an `lbu` reload; widening it to `s32` matched target. decomp_permute improved isolated score but did not provide mutated C, so it was only directional. decomp_diff could not account for contextPatch and inspected a misleading non-patched variant.
- Improvement opportunities: Allow decomp_diff to reuse or accept the contextPatch from the best decomp_attempt so focused diffs are not misleading. Expose Transmuter best mutated source even when full-TU verification fails. Add a stack-layout advisor that recognizes adjacent local slot swaps and suggests declaration-order semantic role swaps before raw pointer/negative-index tactics.
## Chunk 399 — 2026-06-10T21:21:08.524Z
- Matched: no
- Summary: Chunk 399 did not land a match. I analyzed hard float matrix constructor func_15142600 enough to identify its basis-vector math but rotated due low score/large FPR stack mismatch; then reconstructed wrapper func_151623F4 semantics (u8/s8/s16 signature, 32-byte table payload, 6-byte header, func_1516037C + memcpy) but got stuck on an 0x58 vs target 0x50 frame/local placement issue; finally revisited near-miss func_15077F64 and ran Transmuter, which improved isolation score but not full-TU match.
- Likely cause / blocker: func_151623F4 blocker is IDO stack packing: separate locals and single-buffer variants consistently place header/payload/return spill 8 bytes too high or put the return spill after the buffer, while target packs a compiler spill at sp+0x24, header at sp+0x28, payload at sp+0x30 in a 0x50 frame. func_15142600 blocker is hard FPR/register allocation for many float temporaries. func_15077F64 remains a context-sensitive register/frame shaping near-miss.
- Tooling observations: decomp_attempt clearly showed the wrapper source was semantically right (correct byte/halfword reloads, table index math, float stores for zero/D_800A6754) but scores stayed low because the diff scorer treated global 8-byte stack shifts as length/structural mismatches. decomp_permute failed to compile any mutations for pointer-cast-heavy func_151623F4 seeds even with isolate=false, but did improve func_15077F64 in isolation without exposing a candidate source.
- Improvement opportunities: Add a stack-shift normalization mode to decomp_diff/decomp_attempt to recognize identical instruction streams with all locals offset by +8 and identify which local/spill causes frame growth. Improve Transmuter to handle old-C pointer-cast/raw-buffer sources (currently 0 compiled mutations) and always emit best mutated source for isolation improvements, especially for context-sensitive nonmatches.
## Chunk 400 — 2026-06-10T21:23:20.980Z
- Matched: yes
- Summary: Matched and accepted func_15160A58 in game_18D770.c. The successful source builds the header, 0x18-byte payload, and zeroed Header2 stack locals in the target layout, then calls func_151602C0 and memcpy; full ROM SHA gate passed and commit was created.
- Likely cause / blocker: Success came from using a raw payload plus `*(struct17 *)&payload[8] = *arg2` struct assignment for the three-word copy; field-by-field stores either caused f32 conversions or repeated arg2 base reloads. Exact ordering of header stores and payload byte stores was needed for early register/scheduling match.
- Tooling observations: decomp_attempt made the progression clear: first raw field copy was one instruction off but with repeated source reloads; switching to struct assignment produced the target word-copy sequence and showed only scheduling/register diffs; a final reorder of `header.unk2` before `header.unk4` and payload[4] before payload[5] produced the match.
- Improvement opportunities: Add a queue/pattern hint for stack payload copies: when target copies 3 adjacent words via `lw/sw` with a destination pointer temp, suggest struct assignment into a raw payload slice instead of field-by-field copy. Diff output could also highlight when structural_diffs are pure scheduling caused by neighboring store order so the next retry is more obvious.
## Chunk 402 — 2026-06-10T21:32:27.338Z
- Matched: yes
- Summary: Chunk 401 matched and accepted func_1000E46C in init_B1B0.c. I also made substantial progress on func_1502AF04: the simple indexed relocation loop plus pointer local produced the correct DMA alignment, shifted-arg2 stack store, `$s2` aligned DRAM pointer, and IDO modulo-unrolled relocation structure, but it remains a near-miss due loop guard/scheduling variants.
- Likely cause / blocker: func_1000E46C succeeded after reshaping control flow to a final NULL return and writing the percentage scale as compound `arg1 *= 0xFF; arg1 /= 100;`, which produced the target `sll/subu` temp-register divide. `decomp_attempt` showed a false non-match for opcode-equivalent `not` vs `nor`, but `decomp_accept` full ROM SHA passed and committed. func_1502AF04 blocker is the post-DMA relocation loop: simple `for` gives the target modulo-unroll but signed `blez`; explicit `if/do` gives `beqz` but changes remainder scheduling; pointer-increment and unsigned variants perturb the loop too much.
- Tooling observations: decomp_attempt was very effective for func_1000E46C and narrowed it to one alias diff, but it treated `not t2,s0` vs `nor t2,s0,zero` as structural despite identical opcode (`02005027`); decomp_accept correctly relied on full ROM SHA and accepted. Transmuter reached a one-instruction/alias-level isolation result but did not surface candidate source. For func_1502AF04, decomp_diff text alignment was noisy because relocation lines shifted the window, but raw generated asm made the real differences clear.
- Improvement opportunities: Normalize MIPS pseudo-instruction aliases (`not` == `nor rd,rs,zero`, `move` == `or rd,rs,zero`, `li` aliases) before structural diff/match scoring so opcode-identical output can be reported as a match or at least as alias-only. Make Transmuter always emit the best candidate source even when full-TU verification fails, especially when isolation score is 1. Add a loop-shape hint for IDO modulo-unroll cases: simple indexed loops trigger remainder + 4-way unroll, while manual unroll or pointer increments can cause additional unrolling or branch-likely forms.
## Chunk 403 — 2026-06-10T21:36:59.143Z
- Matched: yes
- Summary: Matched and accepted func_1505D5D0 in game_83300.c. Revisited func_1000E46C but in this fresh chunk the alias-only near-match did not pass the full ROM SHA gate, so it was skipped; then explored func_1506D958 and got the semantics mostly clear but not matching due IDO unrolling the known address-range loop.
- Likely cause / blocker: func_1505D5D0 succeeded with a direct float reconstruction: call func_1505D34C with &arg12, scale XZ/Y extents, build dx/dy/dz and radius squared, then call func_1505D408 under the distance check. func_1506D958 is blocked because source forms using for/do/integer-pointer loops all let IDO compute the fixed range from D_800CC5FC to D_800D121C and emit peeled/unrolled loops, while the target is a simple 0x658-stride do/while scan with branch-likely delay-slot loads.
- Tooling observations: decomp_attempt quickly confirmed the exact func_1505D5D0 source once the callee prototype for func_1505D408 was supplied. For func_1506D958, raw generated asm made the unroll issue obvious, but decomp_attempt scores stayed low despite semantically close code because loop unrolling exploded the instruction count. Transmuter improved isolation substantially (156 to 58 instruction differences) but could not bridge the full-TU context-sensitive loop optimization.
- Improvement opportunities: Add an IDO anti-unroll pattern/tool hint for loops over linker-symbol address ranges: source may need volatile end/base declarations, unknown extern pointer indirection, or context patches to prevent IDO from deriving a compile-time trip count. It would help if decomp_attempt diagnosed loop unrolling/peeling specifically when generated code contains div/mod by the stride and repeated loop bodies. Also persist/report Transmuter best candidate source even on full-TU verification failure so improvements like the func_1506D958 isolation forks can be inspected.
## Chunk 404 — 2026-06-10T21:44:44.871Z
- Matched: no
- Summary: Worked on two game_981E0.c functions. func_1506D958 was reshaped from a huge unrolled loop into a close 74-instruction goto-loop near-miss, but remained blocked on branch-likely/register/address scheduling. func_1506BCC8 was reconstructed semantically and narrowed to a single FPU compare operand-order diff (`c.eq.s f2,f0` target vs `c.eq.s f0,f2` generated) after fixing stack frame and D_800D154C address-load shape.
- Likely cause / blocker: No function was accepted. func_1506D958’s blocker is exact branch-likely loop-bottom and hoisted base-pointer/register shaping after suppressing IDO’s loop unrolling via explicit goto. func_1506BCC8’s blocker is a one-instruction symmetric FPU compare operand-order mismatch that persisted across `!=`, `==`, literal-left, zero-local, negated, and assignment-in-condition forms; Transmuter also found no improvement from the 0-instruction-count near-miss.
- Tooling observations: decomp_attempt was very useful for showing raw generated asm; the explicit-goto func_1506D958 attempt revealed an important anti-unroll idiom. decomp_diff remained less useful for high-level alignment when relocation lines inflated early structural diffs, but was useful enough to identify stack-frame and compare-order issues. decomp_permute did not help on func_1506BCC8 despite a one-instruction operand-order mismatch, likely because its mutation set lacks targeted FPU compare operand-order shaping.
- Improvement opportunities: Add a pattern/tool hint for suppressing IDO fixed-range loop unrolling with explicit `goto` labels and bottom loads, and add targeted mutations for branch-likely loop-bottom forms (`if(ptr!=end){load;goto;}` vs `load;if(ptr!=end)goto`). Add a Transmuter rule specifically for FPU compare operand order, including trying zero temp/literal/assignment forms and perhaps detecting symmetric compare single-diffs. Also improve decomp_diff to ignore relocation annotation lines in aligned windows so scores and line numbers reflect actual instructions.
## Chunk 405 — 2026-06-10T21:51:12.552Z
- Matched: no
- Summary: Chunk 405 worked on three candidates. func_15077F64 was revisited via nearMiss routing; a manual dummy-temp variant regressed and Transmuter found an isolated improvement that drifted types (`sh/lhu/lh`) and still did not full-TU match, so it was skipped. func_15070D24 was reconstructed semantically from the family reference and reached a structurally close 0-instruction-count shape, but remained shifted by an 8-byte stack-frame/local-layout gap. func_1506BCC8 was revisited and remains a one-instruction near-miss in its best form, blocked by `c.eq.s f2,f0` vs generated `c.eq.s f0,f2`; new double-literal/truthiness compare forms worsened codegen.
- Likely cause / blocker: No match. Main blockers were exact IDO stack-frame/local-layout shaping for the large local-array function (`func_15070D24`) and FPU compare operand-order shaping for `func_1506BCC8`. `func_15077F64` is heavily attempted and likely needs a fundamentally different type/control-flow strategy for the post-call threshold table block and frame size.
- Tooling observations: decomp_queue nearMiss surfaced a heavily attempted function despite the chunk rule about 8+ attempts; it was useful but easy to spend time on stale shapes. decomp_attempt raw ASM was crucial for discovering that `func_15070D24` semantics are right but locals are shifted; changing `sp64` to `f32[3]` correctly produced target `trunc.w.s` instead of integer copies, and using `2496.0f` produced target `lui 0x451c` instead of a rodata load. decomp_permute could not isolate `func_15070D24` because the required extern types for `D_80099B88/D_80099B94` were not passed into isolation.
- Improvement opportunities: Allow decomp_permute to accept extern declarations like decomp_attempt so functions depending on locally typed globals can be isolated. Add a targeted FPU-compare operand-order mutation/rule for symmetric `c.eq.s` single-diffs. Improve nearMiss routing to optionally exclude functions over the session attempt threshold unless explicitly requested. Add a stack-layout diagnostic that maps C locals to generated stack offsets and suggests declaration/typing changes to shift a whole local block by 4/8 bytes.
## Chunk 407 — 2026-06-10T21:57:39.966Z
- Matched: no
- Summary: Chunk 406 worked on two functions. `func_1502B4A8` was semantically reconstructed as an in-place relocation/mask pass over 8-byte records; manual goto shaping controlled IDO’s over-unroll and got close in instruction count, but it stayed blocked on signed high-bit tests (`bltz` instead of target `and/sltiu`) and branch-likely shape. `func_1000FA64` was reconstructed as an init audio/event slot allocator; an exact-prototype compile failed, then a temporary `arg2` prototype correction from `s16` to `s32` removed the unwanted entry sign-extension and improved the attempt, but IDO still spilled/recomputed the slot pointer instead of using target `$s0`.
- Likely cause / blocker: No match. `func_1502B4A8` needs a way to force unsigned high-bit masking without IDO optimizing to signed `bltz`, plus exact positive-branch shape for pointer relocation. `func_1000FA64` needs callee-saved `$s0` allocation for `struct15 *ptr` and the smaller 0x28 frame; current C spills `ptr` to the stack around calls even when declared `register`.
- Tooling observations: `decomp_attempt` showed that structured `while` caused IDO to over-unroll the relocation loop in `func_1502B4A8` to a huge 166-instruction body; rewriting with explicit labels/gotos reduced it to ~71-77 instructions. `decomp_permute` again failed to use normal attempt history, requiring explicit `code=`; it improved isolation but did not full-TU verify. For `func_1000FA64`, contextPatch was useful to test the likely header prototype correction (`arg2` as `s32`) without committing it.
- Improvement opportunities: Add decomp_permute support for reading full-TU attempt history reliably and for accepting context patches/prototype changes. Add a pattern/rule for preventing IDO from converting `x & 0x80000000` checks into `bltz` when the target uses explicit `and; sltiu`. Add a stack/register diagnostic that explains why a live pointer is spilled instead of allocated to `$s0`, and suggests known C patterns to force callee-saved register allocation.
## Chunk 408 — 2026-06-10T22:04:55.757Z
- Matched: no
- Summary: Chunk 408 switched strategy away from the stalled low-level init/audio family. I first revisited near-miss `func_15077F64`; Transmuter did not full-TU match, and a targeted temporary `D_80099A3C` declaration change from `s16[]` to `u8[]` confirmed the target wants a byte-addressed table (`lbu`) but left only frame-size and temp-register allocation diffs. I skipped the re-served `func_1000FA64` and moved to game-family `func_151452C4`, reconstructing it semantically as a line/sphere intersection routine with two output points and two output parameters; volatile padding forced the exact 0x70 frame, but local struct stack offsets and early copy/load scheduling still diverged.
- Likely cause / blocker: No match. `func_15077F64` is plateaued at the same 0.82 diffs: stack frame size and temp-register allocation around `D_80099A3C` address math. `func_151452C4` semantics are likely correct, but the compiler packs local structs at `sp+0x38/0x44/0x50` while the target uses `sp+0x58/0x4C/0x28`; adding `volatile f32 pad[4]` fixed the frame/argument offsets but did not move local struct offsets. Initial statement order also needs exact interleaving of float differences and struct copies.
- Tooling observations: `decomp_queue` obeyed the strategy-switch filters but many low/medium-low combinations had no candidates; `nearMiss` surfaced an already heavily attempted function, and `familyMode` re-served the previous chunk’s stalled `func_1000FA64`, so manual skip was needed. `decomp_attempt` contextPatch was valuable for testing `D_80099A3C` as `u8[]` and a corrected `func_151452C4` prototype. `decomp_permute` improved isolation scores for both functions but did not provide a full-TU-matching candidate or show the best mutated C in the tool output, limiting follow-up.
- Improvement opportunities: Add queue filters that exclude functions attempted in the immediately previous chunk/session even when familyMode/nearMiss would select them. Extend Transmuter output to include the best mutated C even on non-match, especially when it improves isolation. Add a local-stack-layout advisor that maps generated vs target local object offsets and suggests declaration/padding patterns to move specific structs (e.g. target `sp+0x58/0x4C/0x28` vs generated `sp+0x38/0x44/0x50`).
## Chunk 410 — 2026-06-10T22:13:18.289Z
- Matched: no
- Summary: Chunk 409 deliberately switched away from the repeated hard `func_151452C4` and skipped it when re-served. I worked two medium game candidates: `func_151406AC` in `game_169510.c` and `func_1504ADD0` in `game_77AD0.c`. `func_151406AC` was reconstructed as a `D_800DCE50` bucket linked-list reorder using a stack sentinel; the best raw-pointer form matches the 0x138 frame, initial bucket setup, and inner `slt` + `bnezl` scan shape but is stuck on key/next variable register allocation. `func_1504ADD0` was reconstructed from the commented stub as a 25-object scan with `D_800CC284` selection and `func_150ED578` calls; initializing `i` before the guard fixed the early `$s0`/branch-delay shape, but global-address hoisting and stack buffer placement remain off.
- Likely cause / blocker: No match. `func_151406AC` needs exact register allocation for linked-list locals (`a2` scan-start, `t0/a3` next, `t1` key); Transmuter could not preflight because it needs the local `extern u8 D_800DCE50[]` context that `decomp_attempt` can provide but `decomp_permute` cannot. `func_1504ADD0` is semantically clear but IDO keeps `D_800D154C` in an `$s` register in the natural C while target stores through `$at` inside the loop; attempts to add pointer locals or padding regressed frame size and saved-register allocation. Transmuter improved isolation but did not full-TU match.
- Tooling observations: `decomp_queue` filters for trivial/low/medium-low had no candidates, and `similarToMatched` initially re-served the just-exhausted `func_151452C4`; manual skip was required. `decomp_attempt` extern injection was essential for `func_151406AC`. `decomp_permute` is limited for candidates that only compile with temporary externs/context patches; it failed preflight for `func_151406AC` and only produced an isolation improvement for `func_1504ADD0` without showing the best mutated C.
- Improvement opportunities: Allow `decomp_permute` to accept the same `externs`/`contextPatch` fields as `decomp_attempt`, or reuse the best successful attempt context, so near-matches like `func_151406AC` can be searched. Add a queue exclusion for functions exhausted in the previous chunk to enforce strategy shifts. Add a register-role advisor for loops that maps target live values to generated live values (e.g. `key` in target `$t1` vs generated `$a2`, `next` in target `$a3` vs generated `$t1`).
## Chunk 411 — 2026-06-10T22:15:02.372Z
- Matched: yes
- Summary: Switched away from stalled game/medium families as required. `trivial`, `low`, and `medium-low` filters had no pending candidates, so I changed region to `init` and matched `func_1000E7A0` in `init_B1B0.c` on the first attempt using the existing commented flag-dispatch reconstruction. `decomp_accept` passed the full ROM SHA gate and committed it; progress is now 180/934.
- Likely cause / blocker: Succeeded because the init-region candidate was a direct priority flag dispatcher: direct `if/else if` masks and direct global compound assignments reproduced IDO's branch cascade, address scheduling, and jr-delay-slot stores without extra locals.
- Tooling observations: The queue correctly reported no lower-tier candidates for `trivial`, `low`, and `medium-low`, which justified changing region. The surrounding commented reconstruction for `func_1000E7A0` was accurate enough that `decomp_attempt` matched immediately. `decomp_accept` completed and updated progress cleanly.
- Improvement opportunities: When the no-progress guard fires, queue routing could automatically try a prioritized fallback sequence (`difficulty=trivial`, `low`, `medium-low`, then alternate regions like `init`) and present the first available candidate, reducing manual filter probing. Candidates with commented C stubs that are marked mostly-register-only should be prioritized during stall recovery because some are now exact with minimal cleanup.
## Chunk 413 — 2026-06-10T22:22:14.929Z
- Matched: no
- Summary: Chunk 412 continued the region/family shift after the prior init success. I worked `func_100046E4` in `init_4470.c`: reconstructed it as a DMA chunking helper and found that modeling the `sp+0x64`/`sp+0x68` locals as pointer/raw 32-bit stack slots (not full `OSIoMesg`/`OSMesg` objects) fixes the stack-object interpretation; the best direct-parameter form matches the saved argument register roles and loop shape but is blocked on frame size/slot placement. I then rotated away from oversized hard candidates and used near-miss routing on `func_15077F64`; Transmuter improved isolation to 3 remaining instructions but full-TU did not match.
- Likely cause / blocker: No match. `func_100046E4` is blocked by a delicate IDO stack-layout issue: direct parameter updates give the correct `s2=dramAddr`, `s3=devAddr`, `s6=size` and loop shape, but frame is 0x70/0x78 and stack slots land at `sp+0x68/0x6C`; explicit copy/large-object forms can force 0x80 or `sp+0x64/0x68` but disturb register allocation. `func_15077F64` is a context-sensitive near-miss where Transmuter found a strong isolation candidate but did not expose a full-TU-verifiable source patch.
- Tooling observations: `decomp_queue` successfully served alternate regions, but `region=debugger` produced a 296-instruction hard formatter that was not useful for stall recovery. `decomp_permute` was valuable on `func_15077F64` (55→3 isolation diff, 41 forks) but returned no mutated C, preventing manual full-TU refinement from the best candidate. `decomp_diff` stack focus helped confirm the `func_100046E4` issue is frame/slot layout rather than semantics.
- Improvement opportunities: Have `decomp_permute` print or save the best mutated C even when full-TU verification fails, especially for near-misses with ≤5 isolation differences. Add queue stall-recovery filters that exclude very large hard debugger functions unless explicitly requested. Add a stack-layout explorer that mutates dummy locals/array declarations and reports frame size plus address-taken local offsets for cases like `func_100046E4`. Also enforce the documented auto-rotation for 8+ failed attempts; `func_15077F64` was served with 10 prior attempts.
## Chunk 414 — 2026-06-10T22:27:48.917Z
- Matched: no
- Summary: Chunk 414 focused on `func_1504ADD0` in `game_77AD0.c`. I reconstructed the loop as a 25-entry scan over `D_800CC2D0`, setting `D_800C3E78`, `D_800D154C`, selecting `D_800CC284` from `D_800BE728` or a 6-byte `sp48` bzero buffer, and calling `func_150ED578` for ids `0x28`/`0x77`. The best form is a compact indexed/pointer `for` with `limit=0x19`; it matches the target 0x58 frame and main branch/call structure but does not match saved-register allocation.
- Likely cause / blocker: No match. The best `func_1504ADD0` candidate is blocked on IDO saved-register shaping: target wants `s0=i`, `s1=tmp`, `s2=&D_800CC284`, `s3=sp48`, `s4=0x19`, `s5=&D_800C3E78`, `s6=&D_8008FD8C`, `s7=&D_800BE728`, `fp=0x28`, with `D_800D154C` stored via `$at` inside the loop. Generated variants either put `sp48` in `s2` and `D_800CC284` in `s3`, or hoist `D_800D154C` into a saved register, shifting `D_800BE728/D_8008FD8C/D_800C3E78` upward and inflating the frame to 0x60. Transmuter improved isolation but did not find a full-TU match.
- Tooling observations: `decomp_queue` initially served a good medium candidate, but after skipping it the small/low/similarity filters had no candidates and family mode routed to a 180-instruction hard function, which was not useful for chunk recovery. `decomp_attempt` diffs were enough to identify exact target register roles, but `decomp_permute` again reported an isolation improvement without exposing the mutated source, limiting manual refinement.
- Improvement opportunities: Add a saved-register-role explorer for IDO that can mutate loop header form, local pointer hoisting, and constant locals while reporting the assigned saved registers before full diffing. Improve `decomp_permute` to emit the best mutated C on failed full-TU verification. Add queue filters for 'medium only' or 'exclude hard >N instructions' fallback so family routing doesn't jump from a 73-instruction near-match to a 180-instruction hard function.
## Chunk 416 — 2026-06-10T22:39:51.149Z
- Matched: no
- Summary: Chunk 416 followed the required strategy adjustment: skipped the stale near-miss `func_15077F64`, tried a different region (`debugger`) but skipped a 369-instruction hard formatter, then attempted init `func_1000C7E8` and finally medium game `func_150727F0`. No function matched, but `func_150727F0` was reconstructed semantically as a four-at-a-time scan over actor slots from `D_800CC5FC` to `D_800D121C`, clearing `unk218` and copying `D_800D1580` to `unk232` when `D_800C3E78 + 1` matches `unk65`, including the special first-slot globals `D_800CC4E8/D_800CC502`.
- Likely cause / blocker: `func_150727F0` is blocked on final IDO scheduling/register shape. The best practical C uses raw pointer arithmetic, `end` before `ptr` for `v0=end`/`v1=ptr`, and volatile pointer locals for `D_800C3E78`/`D_800D1580` to hoist them into `a0/a1`; it matches length and loop structure but differs in initial temp-register names and in the body order (`sw zero` before `lw *val` instead of target `lw *val`, `sw zero`, `sb`). Introducing a temp or comma expression fixes load order locally but triggers IDO strength-reduction / extra unrolling to 172 instructions. `func_1000C7E8` was blocked by an ABI/codegen issue where C homes the third float-ish argument to stack and reloads it, while target does entry `mtc1 a2,$f14`.
- Tooling observations: `decomp_queue` obeyed the region/difficulty switch, but debugger/init filters still surfaced hard candidates; medium game routing gave a much more productive no-call target. `decomp_attempt` made it clear when volatile pointer locals successfully forced address hoisting. `decomp_permute` found isolation improvements for both `func_1000C7E8` and `func_150727F0` but again did not expose the mutated C, which would have been especially useful for the 5-diff table-scan candidate.
- Improvement opportunities: Add a queue filter for maximum instruction count together with region/difficulty so strategy changes do not land on huge hard candidates. Add a Transmuter option to print best failed full-TU source and the isolated source that scored best. Add a targeted mutation family for no-call table scans that toggles raw pointer integer types, volatile pointer hoists, assignment order (`lw value` before zero-store), and prevents IDO from strength-reducing/unrolling fixed end-pointer loops.
## Chunk 417 — 2026-06-10T22:52:51.505Z
- Matched: no
- Summary: Chunk 417 followed the required strategy shift: exhausted low/trivial/debugger capped filters, then used family/similarity routing. Tried `func_151670C0` (same-file callback-table walker), revisited stale near-miss `func_15077F64` only via Transmuter, and tried high-similarity hard float updater `func_1513E6E8`. No match was accepted.
- Likely cause / blocker: `func_151670C0` was reconstructed semantically as a two-pass, 101-entry callback/list walker over `D_800DCE50`, `D_8008B4A8`, `D_800DD198`, and `D_800DD190`; best hand attempt is close structurally but blocked by IDO saved-register allocation (`D_800DCE50` in `$fp` vs target `$s7`, limit in `$s7` vs target `$fp`) and by stores to `D_800DD190` using `%hi/%lo` instead of the hoisted `$s3` pointer. `func_1513E6E8` was reconstructed semantically as flag-gated float integration using `D_800BE9A4`; blocked by IDO folding `arg0+0x110` into direct `a0+offset` accesses instead of materializing `$v0 = arg0 + 0x110`, plus scheduling/FPR order. `func_15077F64` remains stale; Transmuter improved isolation but no full-TU match.
- Tooling observations: `decomp_queue` with strict low/trivial/debugger caps returned no candidates, while `familyMode` and `similarToMatched` provided meaningful references but hard 70-85 instruction functions. The nearMiss filter still returned `func_15077F64` despite a prior strategy instruction to avoid stale candidates and despite 10 attempts; using Transmuter avoided repeating hand attempts. `decomp_attempt` was effective for isolating IDO register allocation effects in `func_151670C0`; `decomp_permute` again reported improvements without showing the source, limiting usefulness.
- Improvement opportunities: Add a queue filter or session memory to exclude named stale functions from nearMiss routing after explicit skip, or deprioritize candidates with >8 attempts unless explicitly requested. Add Transmuter output of best failed full-TU source and isolated-best source. Add mutation support for hoisted scalar-global pointers (`s8 *depth=&GLOBAL` stores through pointer vs direct `%hi/%lo`) and for forcing derived base pointers (`v0=arg0+0x110`) instead of folded `a0+offset` float accesses.
## Chunk 418 — 2026-06-10T23:03:45.936Z
- Matched: no
- Summary: Chunk 418 switched away from the previous hard/similarity route as requested. Low/trivial/init/debugger filters were empty, so I worked new medium game families: `func_150727F0` (object-slot cleanup loop in `game_981E0.c`) and `func_151671E8` (callback-table walker sibling in `game_1944C0.c`). No match was accepted.
- Likely cause / blocker: `func_150727F0` was semantically reconstructed as a grouped loop over 4 `struct127` slots per iteration from `D_800CC5FC` to `D_800D121C`, clearing `unk218`/setting `unk232` when `D_800C3E78+1` matches `unk65`, plus a special `D_800CC2D0` pointer case. Best stable score was ~0.457; removing the u8 mask caused IDO to constant-span/unroll the loop into ~180 instructions. `func_151671E8` was reconstructed as the same two-pass, 101-entry object-list walker as `func_151670C0`, but using callback slot `D_8008B4A8 + 4`; best attempt was a 75-vs-74 instruction near-miss but still wrong saved-register/global-hoist shape and direct `%hi/%lo` stores for `D_800DD190`. Transmuter did not produce a full-TU match for either family.
- Tooling observations: Queue filters confirmed that trivial/low/medium-low candidates are depleted across game/init/debugger; unfiltered/medium game produced fresh non-stale functions. `decomp_attempt` with contextPatch for `D_800CC2D0` as a pointer was essential for `func_150727F0`. Transmuter again improved isolation scores but did not expose the mutated source, so the remaining full-TU mismatch was not actionable.
- Improvement opportunities: Add a way for Transmuter to run with the same `contextPatch` used by `decomp_attempt`, or to record context-patched attempts as baseline history; for `func_150727F0`, Transmuter initially reported no history despite several context-patched attempts. Add diagnostics for when IDO constant-span loop reasoning/unrolling is triggered by minor compare-type changes. Add a mutation/pattern to force hoisted `s8 *GLOBAL` stores through the pointer instead of `%hi/%lo` direct stores in the callback walker family.
## Chunk 419 — 2026-06-10T23:14:53.404Z
- Matched: no
- Summary: Chunk 419 intentionally changed routing after the no-progress streak: tried `nearMiss`, hard/similar game, debugger, and finally returned to the smallest unfiltered pending function. No function was accepted. Work focused on `func_15077F64` (near-miss), `func_15070F60` (hard game, skipped as too large), `func_16001BB4` (debugger printf-like 402-instruction parser, skipped), and `func_151671E8` (callback walker near-miss).
- Likely cause / blocker: `func_15077F64` remains blocked on frame/register shaping despite a raw stack-buffer rewrite and Transmuter; the rewrite worsened frame size to 0x40. `func_15070F60` and `func_16001BB4` were different families but too large for a productive chunk. For `func_151671E8`, moving stride math inside the callback-present branch improved the shape: it now hoists `D_800DCE50` into `$s7`, stack into `$s4`, depth pointer into `$s3`, and keeps `$s6` as the outer counter, but IDO still emits two extra `%hi(D_800DD190)` stores instead of `sb ...,0($s3)` for the depth increment/decrement. Transmuter did not find a full-TU match.
- Tooling observations: `decomp_queue next` with `nearMiss:true` served a 10-attempt function despite the instruction saying 8+ failed attempts are auto-rotated; manual skip was needed. `promote` without a function only listed segments, which is useful but not enough to automatically unlock easier candidates. Transmuter improves isolated scores but still does not return the best mutated source, limiting follow-up when full-TU verification fails.
- Improvement opportunities: Add an auto-route that excludes high-attempt functions for nearMiss mode or clearly marks them as override candidates. Add a queue filter for instruction ranges above the depleted low tier (e.g. 40-80) plus no recent attempts to avoid huge 180/402-instruction functions. Add a Transmuter option to emit the best full-TU nonmatching source, not just the score/rules. Add an IDO pattern/mutation for forcing byte global stores through a live pointer register (`sb reg,0(s3)`) instead of direct `%hi/%lo` stores when the pointer is already loaded.
## Chunk 420 — 2026-06-10T23:29:39.076Z
- Matched: no
- Summary: Chunk 420 switched to the init region per strategy adjustment and worked on `func_1000B060`, an 84-instruction float/angle helper in `init_A420.c`. No accepted match, but a strong near-miss was developed from the existing sketch: inline `sqrtf`, stack halfword flag at `sp+0x1C`, `$a1` flag lifetime, target FPR allocation (`mul.d $f4`, `mtc1 $f8`, `trunc.w.d $f6`), and `$a0` integer conversion were all achieved in variants.
- Likely cause / blocker: The remaining blocker is final signed-byte range-shaping/register allocation. The best direct-cast variant is one instruction short (83 vs target 84) because IDO folds `(phi + arg2)` into `$v1` directly, while target computes a separate signed-byte temp (`addu` into a temp, `sll`, `sra`), then assigns it to `$v1` in a branch delay slot. Variants with `s8`/`s32 temp_t9` reintroduced duplicate sign-extension or moved the flag from `$a1` to `$a3`. Transmuter improved isolated scores but found no full-TU match.
- Tooling observations: `decomp_attempt` raw ASM was essential because `decomp_diff` alignment is confused by relocation annotation lines and reports misleading generated lengths/windows for this file. Transmuter again reports improved isolated scores but not the mutated source, which limits manual follow-up when full-TU verification fails.
- Improvement opportunities: Improve `decomp_diff` to strip/ignore relocation annotation lines before alignment so target/generated windows stay synchronized. Add a Transmuter option to dump the best nonmatching C source for full-TU follow-up. Add/codegen-learn a pattern for IDO final byte-range ladders: using a live zero padding halfword (`pad = 0; return ... | pad`) can shift a scalar `s16` flag from `sp+0x1E` to `sp+0x1C` while preserving `$a1`; using `s32 temp_a0 = double_expr` preserves target FPR allocation and emits `mfc1 $a0`.
## Chunk 421 — 2026-06-10T23:44:08.112Z
- Matched: no
- Summary: Chunk 421 obeyed the strategy adjustment by leaving the stalled init near-miss path. Tried `func_1513E6E8` (hard same-file float integrator) and developed a structurally close volatile-global/volatile-flags version, then switched again to `func_151671E8` (medium callback/list dispatcher) and reconstructed the full algorithm with nested loops, callback dispatch, `D_800DD190` stack-depth byte, `D_800DD198` next-pointer stack, and `D_800DCE50` 2D table indexing.
- Likely cause / blocker: No match. `func_1513E6E8` blocked on IDO register allocation/base folding: generated code keeps flags in `$v0` and global pointer in `$v1`, while target wants flags `$v1`, global pointer `$a1`, and explicit `addiu $v0,$a0,0x110` bases. `func_151671E8` got very close algorithmically and restored target 2D table offset math using `(s32 (*)[0x68])D_800DCE50`, but remained off on `$fp/$s7` allocation (generated table in `$fp`/limit in `$s7`, target table in `$s7`/limit in `$fp`) and `jalr` delay-slot scheduling (generated `nop` unless callback temp is reintroduced, which worsens saved-register allocation).
- Tooling observations: `decomp_attempt` was again the useful source of truth; `decomp_diff` alignment is noisy around relocation annotation lines but the structural summaries still exposed register allocation. Transmuter improved isolated scores for both attempted functions but no full-TU matches, and the lack of best-mutant source prevents learning from its partial improvements.
- Improvement opportunities: Add a Transmuter mode to emit best nonmatching source even when full-TU verification fails. Add a targeted IDO mutation rule for swapping `$fp`/`$s7` long-lived locals (e.g. limit constant vs hoisted table base). Improve `decomp_diff` relocation filtering/alignment. Consider a specialized helper/skeleton generator for nested callback-list walks with byte stack depth globals and branch-likely loops.
## Chunk 422 — 2026-06-10T23:56:11.577Z
- Matched: no
- Summary: Chunk 422 switched strategy to init hard similarity-routed functions. For `__n_seqpStopOsc`, reconstructed an essentially exact raw-memory C implementation: all generated instructions match target except the original asm includes one extra trailing `nop` after the `jr ra` delay slot at end-of-TU. Then skipped it and tried `func_10011310`, deriving the global compaction/freeing algorithm and improving copy codegen by temporarily correcting `D_80041F10` from pointer-array to packed `struct49[]` and using struct assignment to get `lwl/lwr/swl/swr`.
- Likely cause / blocker: No accepted match. `__n_seqpStopOsc` is blocked solely by anonymous trailing padding: C emits 114 instructions, target asm has 115 with a final padding nop after epilogue. Inline/top-level asm forms are unsupported by IDO here (`asm`/`__asm__` compile as calls or syntax errors), and GLOBAL_ASM rejects a one-instruction anonymous block/needs glabel. `func_10011310` remains a register/scheduling mismatch: generated keeps the address of `D_80041F50` in a saved register (`s8`) and uses `v1` for the count, while target loads count into `$a1`, final-stores via `$at`, keeps D_80041F10 bases in `$s1/$s6`, D_800425E0 in `$s7`, and uses `$fp=0xC`.
- Tooling observations: `decomp_attempt` surfaced that `__n_seqpStopOsc` C was byte-for-byte except padding, but the score was misleadingly low (~0.496) because formatting/length alignment dominated. `decomp_permute` was unhelpful on that exact function because isolation scoring saw many differences despite full-TU being one trailing nop off. `GLOBAL_ASM` diagnostics showed the asm processor refuses anonymous/too-short padding blocks, which blocks decomping end-of-TU functions with padding nops.
- Improvement opportunities: Add tool support for end-of-function/end-of-TU padding nops: either let `decomp_attempt/decomp_accept` tolerate linker/assembler padding when all semantic instructions match, or provide a sanctioned anonymous padding pragma/metadata mechanism. Improve diff scoring to recognize 'only trailing padding nop differs' as a near-perfect match. For `func_10011310`, add a skeleton/shaping recipe for packed 4-byte struct-array compaction loops and a mutation that avoids saving global addresses in `$s` registers when target reloads globals into caller-saved regs.
## Chunk 27 — 2026-06-12T12:49:04.810Z
- Matched: no
- Summary: Attempted 2 functions (func_15077F64, func_151415D4, func_16001700) over 3 chunks. All have complex branch-likely patterns (bc1fl) that resist standard C codegen. func_15077F64: 13 prior attempts, best score 0.83, plateaued. Transmuter found no improvements. func_151415D4: 6 prior attempts, best 0.493, all stuck on bc1fl delay slot pattern. func_16001700: basic structure attempted but needs refinement. Key blocker: IDO 5.3's bc1fl generation requires specific C patterns not yet identified.
- Likely cause / blocker: Complex branch-likely (bc1fl) floating-point branches that require delay-slot fills. The C code cannot easily express 'load in delay slot that executes if branch NOT taken' pattern that IDO generates. All attempts with standard if/else, do/while, for loops, and various temp variable arrangements failed to produce bc1fl.
- Tooling observations: decomp_queue with filter={nearMiss:true} correctly identified func_15077F64 as a permuter candidate (score 0.83), but Transmuter was unable to fix the last 5 diffs - all were branch-related (wrong registers in address calculation for D_80099A3C access). The permuter tried 3400 iterations without success. This suggests the issue is structural (declaration order or C pattern) rather than simple mutation. For func_151415D4, score was consistently ~0.49 across 6 attempts - length mismatch indicates missing/extra branches. The skeleton seed from func_1514182C was not useful (different structure entirely).
- Improvement opportunities: 1) Pattern discovery needed: 'float-branch-likely-with-load-delay-slot' - a specific C idiom (possibly involving comma operators or specific comparison ordering) that forces IDO to emit bc1fl with lwc1 in delay slot. 2) Transmuter could be enhanced to specifically mutate branch condition polarity and delay slot scheduling for floating-point branches. 3) Consider allowing inline asm for branch-likely patterns after N failed attempts on functions where this is the only diff. 4) decomp_diff focus=branches analysis could specifically identify bc1fl vs bclf differences and suggest C rewrites.
## Chunk 28 — 2026-06-12T12:55:57.026Z
- Matched: no
- Summary: Chunk 28: Attempted 2 functions (func_1000E2F4 and func_151415D4) over 8 decomp_attempt calls and 1 Transmuter run. Both functions stuck on branch-likely patterns where IDO's code generation differs from C control flow. func_1000E2F4: 5 attempts, plateaued at ~0.43, issue is complex nested conditionals with branch-likely (bnel). func_151415D4: 6 attempts, plateaued at ~0.49, issue is floating-point bc1fl branches with load-in-delay-slot patterns. Both require identifying specific C patterns that force IDO to emit specific branch-likely instructions.
- Likely cause / blocker: Branch-likely patterns (bnel for int compares, bc1fl for float compares) with specific delay slot fills that don't map cleanly to standard C control flow. The C compiler cannot express 'execute this instruction only if branch NOT taken' semantics that MIPS branch-likely provides.
- Tooling observations: Transmuter on func_1000E2F4: 3438 iterations, best improvement from 64 to 29 diffs (127 forks used), but could not bridge the gap. This confirms branch-likely issues are structural, not simple expression mutations. decomp_queue with filter={region: init} was useful for finding a function with different characteristics, but both init and game regions have branch-likely challenges.
- Improvement opportunities: 1) Research and document IDO 5.3 branch-likely patterns: specific C constructs that generate bnel/bc1fl. 2) Transmuter should prioritize branch condition inversion and delay slot reorganization for functions with branch-likely mismatches. 3) Consider manual __asm__ insertion for confirmed branch-likely-only differences after thorough Transmuter search. 4) decomp_diff focus=branches could provide specific branch-likely transformation suggestions.
## Chunk 29 — 2026-06-12T13:01:22.757Z
- Matched: no
- Summary: Chunk 29: Attempted func_15077F64 (game_A28B0.c) which has 16+ prior attempts. This function has complex branch-likely patterns (bnel) that are difficult to match. Attempted different approaches including changing stack variable arrangements and using different control flow patterns, but the function remains stuck around 0.49-0.83 score range. The key blocker is the branch-likely at the end where the condition needs to execute a lw in the delay slot on branch-not-taken, which standard C cannot express.
- Likely cause / blocker: Branch-likely patterns (bnel) in MIPS assembly cannot be expressed in standard C. The target uses bnel with lw in the delay slot, which requires 'execute only if branch NOT taken' semantics that IDO 5.3 cannot generate from standard conditionals.
- Tooling observations: decomp_queue with filter={nearMiss:true} returned func_15077F64 which had extensive prior work (16 attempts) but couldn't be matched. After 9 chunks of no progress, moving to fresh candidates with filter={difficulty: trivial/low} might yield better results. The global progress shows 232/934 matched now, so there's still substantial work remaining.
- Improvement opportunities: 1) Document specific IDO 5.3 patterns for bnel generation - research what C constructs can produce this. 2) For functions dominated by branch-likely patterns, consider documenting them as 'assembly-required' or investigate if inline assembly is acceptable. 3) Transmuter has already been run extensively on this function (3400+ iterations) with limited success - this confirms branch-likely is a fundamental mismatch. 4) Consider promoting segments that contain only such patterns to unlock other decompilable functions.
## Chunk 32 — 2026-06-12T13:41:02.971Z
- Matched: no
- Summary: Chunk 32: Attempted multiple functions (func_16001700, func_151415D4 via permuter, func_1000E2F4) all with branch-likely patterns. The init function func_1000E2F4 has complex nested conditionals with multiple branch-likely instructions (beql, blezl, bnel) that are difficult to express in standard C. Best score achieved was 0.46. The debugger function func_16001700 and game function func_151415D4 also exhibit similar branch-likely challenges.
- Likely cause / blocker: Multiple functions in this chunk suffer from branch-likely (bnel, beql, blezl) patterns that require specific C constructs to generate correctly. IDO 5.3's branch-likely generation is sensitive to condition ordering and nested if-else structures.
- Tooling observations: decomp_queue with filter={region: debugger} and filter={region: init} returned functions that still have complex branch patterns. The queue auto-promotion seems to be surfacing difficult functions. Consider adding filter to exclude functions with branch-likely instructions, or prioritize very simple leaf functions (no branches at all) to break the dry spell.
- Improvement opportunities: 1) Research and document IDO 5.3 branch-likely patterns more extensively - specifically what C constructs generate beql vs beqzl, bnel vs bnezl, blezl. 2) Consider that functions with 4+ branch-likely instructions may need alternative approaches or documentation as 'assembly-only' patterns. 3) Transmuter has been run on these functions with limited success, suggesting the issue is fundamental C-to-assembly semantic gap. 4) After 12 chunks of no progress, queue routing should strongly prioritize functions WITHOUT branch-likely instructions.
## Chunk 33 — 2026-06-12T13:49:37.673Z
- Matched: no
- Summary: Chunk 33: Attempted func_15077F64 with 17 prior attempts, best score 0.83. Tried permuter (reached 5 instruction diff). The function involves complex register allocation for globals D_800D154C, D_800D2104 and manual address computation for D_80099A3C. The main blocking issues are: 1) Stack frame size (target -40, best achieved -32), 2) Register allocation for global address loading (t5/t6/t8 usage), 3) Signed vs unsigned byte handling for temp_t6 decrement.
- Likely cause / blocker: Complex global variable access patterns with specific register allocation requirements. The function loads multiple globals (D_800D154C, D_800D2104, D_800D1890, D_80099A3C) and IDO is not allocating the target registers ($t6, $t5, $t8) as expected. Also stack frame padding is not matching.
- Tooling observations: decomp_permute reached 5 instruction diff but couldn't bridge the gap. The issue appears to be fundamental C-to-assembly semantic gaps that Transmuter cannot resolve through AST mutations alone. The global variable access patterns and register allocation are the primary blockers.
- Improvement opportunities: 1) Need better patterns for manual register allocation control when accessing multiple globals. 2) Consider documenting this function as requiring specific stack layout (-40 frame with specific padding). 3) After 14 chunks of no progress, the queue routing should avoid functions with complex global variable access patterns and branch-likely instructions. 4) Consider adding a filter to prioritize functions with simple stack frames (no padding) and minimal global accesses.
## Chunk 35 — 2026-06-12T14:16:37.295Z
- Matched: no
- Summary: Chunk 34-35: Attempted func_151415D4 extensively. Verified ROM is healthy (matched). Function has a complex while-loop with floating-point operations that's generating 68 instructions instead of 69 target. Transmuter improved to 37 diffs but couldn't bridge the gap due to context sensitivity. The loop pattern involves: load temp_v0->unkC, add D_800BE9A4, store, reload, compare with temp_f0, then subtract-in-place in a bc1tl loop. After 16+ attempts, moving on.
- Likely cause / blocker: Complex floating-point while-loop with specific IDO register allocation for loop-carried dependencies. The target has a specific pattern of load-add-store-reload-compare-subtract-store that IDO compiles differently than the C while-loop structure. This is a common IDO codegen issue with iterative floating-point loops.
- Tooling observations: decomp_permute improved from 40 to 37 but auto-verified as non-matching in full TU due to context sensitivity. The permuter is effective for local instruction scheduling but cannot fix structural codegen differences in IDO's floating-point loop handling.
- Improvement opportunities: 1) Document this pattern: while-loop with floating-point accumulators and comparison often requires specific temp variable ordering or volatile hints. 2) After 15 chunks of no progress, consider prioritizing functions without complex floating-point loops. 3) The permuter reached 37 diffs but couldn't match - suggests the issue is in fundamental C structure vs assembly, not just statement ordering.
## Chunk 77 — 2026-06-12T16:09:44.100Z
- Matched: no
- Summary: 57 consecutive chunks with zero progress. Attempted func_15007440 (game_33990.c, 9 attempts, best 0.49), func_15077F64 (game_A28B0.c, 18 attempts, best 0.83 - stuck on stack frame/register allocation), func_151623F4 (game_18D770.c, multiple attempts with compile errors due to wrong func_1516037C signature), func_1000E46C (init_B1B0.c, compile error from wrong func_10008790 arg count), func_16001700 (debugger_256F80.c, 3 attempts, best 0.38 - redeclaration conflicts). Core blockers: incorrect function prototypes, struct field mismatches, and IDO codegen sensitivity on stack/layout differences.
- Likely cause / blocker: 57 chunks with zero progress. All attempted candidates (func_15007440, func_15077F64, func_151623F4, func_1000E46C, func_16001700) failed due to: wrong function signatures/prototypes, struct field mismatches, incorrect call argument counts, and IDO codegen differences. The queue keeps cycling the same difficult medium functions with no trivial/low difficulty options available.
- Tooling observations: decomp_attempt frequently fails at compile stage due to wrong function signatures (argument count/type mismatches with existing declarations). The queue lacks trivial/low-difficulty candidates. mips_to_c was never utilized despite being listed as a low-level aid. Transmuter runs improved scores slightly but context-sensitivity prevented full matches.
- Improvement opportunities: 1. mips_to_c tool should be used first to get mechanical C translations before manual attempts - this was never tried. 2. Queue should prioritize functions with known-correct prototypes (from functions.h) to avoid compile errors. 3. A filter for 'familyMode' with recently-matched siblings would help find functions with proven struct/call patterns. 4. Consider running decomp_queue promote to unlock new candidates from .s segments. 5. The tooling-refinement log needs updating with the specific prototype/signature errors encountered so future attempts avoid them.
## Chunk 502 — 2026-06-12T20:42:26.737Z
- Matched: no
- Summary: Exhausted reasonable options for func_1000E46C. Attempted various control flow structures and stack layouts, but could not match the target assembly. Plateaud around 0.49 score. Likely cause: complex IDO 5.3 register allocation and branch delay slot filling that is context-sensitive. Tooling improvement: better similarity references for these kinds of initialization functions.
- Likely cause / blocker: not provided
- Tooling observations: not provided
- Improvement opportunities: not provided
## Chunk 509 — 2026-06-12T21:09:28.813Z
- Matched: no
- Summary: Exhausted reasonable attempts for func_1502AF04 without achieving a matching decompilation. The function exhibits tight loop control flow and precise pointer arithmetic tied to global indices; mismatches stem from stack layout and register reuse nuances hard to capture in isolated C attempts. Likely cause/blocker: tight coupling to game-specific globals (D_800A670C, D_800A6730) and macro-like index calculations. Concrete tooling improvement opportunities: better similarity reference generation for index-based loops; enhanced handling of scaled pointer arithmetic in IDO 5.3 reconstruction.
- Likely cause / blocker: not provided
- Tooling observations: not provided
- Improvement opportunities: not provided
## Chunk 514 — 2026-06-12T21:29:22.855Z
- Matched: no
- Summary: Skipped func_1502AF04 after 3+ failed attempts and Transmuter plateau at 70. Root cause: stack frame mismatch (target -0x40 vs generated -0x28) and control flow structure differences prevent IDO register scheduling match. Tooling opportunity: stack-depth control patterns for loop-heavy functions; better similarity weighting to avoid repeated failing candidates.
- Likely cause / blocker: not provided
- Tooling observations: not provided
- Improvement opportunities: not provided
## Chunk 517 — 2026-06-12T21:44:40.963Z
- Matched: no
- Summary: Tried three functions this chunk: func_1502AF04 (game_57FA0.c, 71 instr), func_1000E46C (init_B1B0.c, 71 instr), and func_151623F4 (game_18D770.c, 71 instr). All plateaued around 0.4-0.5 score with length mismatches (target 71 vs generated 59-72). Root cause: stack frame allocation differences, branch-likely pattern mismatches, and register allocation (s0/s1 usage) not matching target. Tooling opportunity: better similarity filtering to avoid revisiting same plateaued functions; need pattern for branch-likely epilogue merging; better control flow templates for loop+conditional structures.
- Likely cause / blocker: Stack frame allocation differences, branch-likely pattern mismatches, and register allocation (s0/s1 usage) not matching target.
- Tooling observations: Similarity filter keeps returning same plateaued functions; branch-likely pattern not in library; decomp_permuter fails to compile mutations due to isolation.
- Improvement opportunities: Add branch-likely pattern to library; improve near-miss filtering to exclude functions with >5 attempts; add stack-depth control pattern for IDO.
## Chunk 525 — 2026-06-12T22:35:25.061Z
- Matched: no
- Summary: Attempted several functions in this chunk: func_1000B060 (compile error), func_151406AC (skipped), func_16001700 (multiple attempts, length mismatch), func_15077F64 (skipped), func_1504ADD0 (multiple attempts, length mismatch, transmuter tried), func_151347CC (current candidate, not yet attempted). No matches achieved. Need to adjust strategy: try different region/difficulty filters, use similarity to matched functions, or focus on trivial/low difficulty functions to break the streak.
- Likely cause / blocker: Persistent length mismatches and compilation errors; inability to converge on correct C code despite multiple attempts and transmuter use. Likely need to reconsider function selection or decompilation approach.
- Tooling observations: Tools reliably detect length mismatches and provide transmuter for near-misses, but when structural differences are large (e.g., wrong control flow), transmuter gets stuck in local minima. Need better guidance for selecting functions with higher match probability.
- Improvement opportunities: Integrate static analysis to predict function complexity (e.g., cyclomatic complexity) and prioritize simpler functions. Enhance candidate queue ranking with similarity to recently matched functions. Provide more detailed diff analysis for structural mismatches (e.g., control flow graph differences).
## Chunk 527 — 2026-06-12T22:47:08.726Z
- Matched: no
- Summary: Attempted func_16001700 (debugger_256F80.c) 4 times with varying signatures and code structures. Each attempt resulted in length mismatch (target 76 vs generated 37-101 instructions) or compilation errors. Permuter improved score from 48 to 40 but still no match due to context-sensitivity. Likely the function requires precise register allocation and interaction with OS-specific globals that are not captured in simplified C models. Consider moving to a different candidate or adjusting queue filters.
- Likely cause / blocker: Persistent length mismatches and inability to align control flow despite multiple signature adjustments and permuter attempts. The function's complex interaction with OS-specific globals and multi-phase loops may require deeper analysis of the surrounding context or a different function family.
- Tooling observations: Tools reliably detect mismatches and provide transmuter improvements, but structural differences (e.g., multi-phase loops, OS-specific side effects) remain unresolved.
- Improvement opportunities: Investigate the full TU context for this function, examine how OS-specific globals are used, and consider enabling more detailed debugging or alternative decompilation strategies.
## Chunk 529 — 2026-06-12T22:53:01.090Z
- Matched: no
- Summary: Exhausted reasonable options for func_151347CC (game_161520.c) after 4 attempts, permute, and multiple filter variations. Persistent length mismatches (target 73 vs generated 64) and structural differences in control flow suggest deeper context-sensitivity or fundamental mismatch in algorithmic approach. No viable candidates remain in current queue filters.
- Likely cause / blocker: Persistent structural mismatches and inability to align control flow despite multiple signature adjustments and permuter improvements. The function's complex conditional branching and OS-specific register interactions may require fundamentally different decompilation strategy or auto-rotation to next candidate.
- Tooling observations: Tools reliably detect mismatches and provide transmuter improvements, but structural differences (e.g., multi-phase conditionals, OS-specific side effects) remain unresolved.
- Improvement opportunities: Enable auto-rotation to skip this function and try different region/difficulty filters in next chunk. Consider analyzing full TU context for this function or switching to a different function family.
## Chunk 530 — 2026-06-12T23:05:01.607Z
- Matched: no
- Summary: Repeated attempts for func_151347CC have consistently produced a length mismatch error. The target uses a tightly‑packed switch extended branch‑likely sequence and performs several loads of the same field with a 'volatile' pattern that our current C templates miss. All recent attempts (including a full Transmuter search) returned code that either over‑sized or under‑sized the generated prologue/epilogue. The core difficulty appears to be matching the conditional load+jump pattern for the case label 0x11/0x16 and correctly aligning the extra '0x18' field access when the branch is taken.  The length mismatch persists for 7 attempts.

We attempted: a direct copy of the logic; a simplified condition set; sequential if/else with explicit branches; use of 'or' in condition; grouping the 0x11/0x16 branches; calling the helper immediately; and a minimal switch. Each time the generated assembler differed by 3 ‑ 10 instructions.

Given the repeated failure, the function is likely auto‑rotated out of the current chunk or should be replaced by an alternate strategy.  It may require a more radical rewrite, e.g., using a helper that performs the repeated load in a volatile fashion or converting the control flow into a flat if‑chain followed by a tail‑call style.  Switching to a higher‑level decompilation (greedy) or decomp_queue filt=mask may yield an easier target.

Bottom line: the current candidate is not matchable with our present strategies and should be skipped or deferred to a novel approach such as a hand‑crafted decompiler or re‑enabling a lower‑difficulty region.

- Likely cause / blocker: Unable to reconcile branch‑likely and return patterns; consistent length mismatches (target=73, generated ~70–82).
- Tooling observations: not provided
- Improvement opportunities: 1) Re‑write the function as a flat if‑chain with explicit 'volatile' loads to match the repeated field access. 2) Split the logic into helper functions to reduce register pressure. 3) Explore using a manual IL translation to emulate the target's conditional jump style (e.g., use a 'ldc' pattern). 4) Enable morphological patterns such as `volatile-field-condition-reload` and `combine-global-u8-increment-expression` in the IDO rules. 5) Try a higher depth Transmuter run or Conquer‑by‑patterns search on a per‑branch basis.
## Chunk 540 — 2026-06-12T23:49:22.655Z
- Matched: no
- Summary: Exhausted reasonable options for func_151F7F60 (74 instructions, medium difficulty). Multiple attempts with syntax errors and compilation failures prevented progress. Key blockers: 1) Global variable redeclarations in C code causing syntax errors 2) Complex pointer arithmetic in assembly requiring precise C translation 3) Need for proper struct definitions from included headers. No viable C implementation found after 3+ attempts. Next step: Switch to different function family/region as per strategy adjustment.
- Likely cause / blocker: Multiple syntax errors in C code due to global variable redeclarations and missing struct definitions. Compilation failures prevented testing of C implementation. Function requires precise pointer arithmetic and struct field access that cannot be resolved without full type definitions.
- Tooling observations: IDO patterns show strong register allocation and stack management requirements. Assembly uses complex pointer arithmetic with multiple indirections. Pattern matching suggests potential for improvement in stack frame organization and register reuse.
- Improvement opportunities: 1. Implement better global variable management in decompilation to avoid redeclaration conflicts
2. Develop more robust struct reconstruction from assembly references
3. Improve pointer arithmetic handling in C translations for complex addressing modes
4. Create more targeted decompilation filters for medium-difficulty functions with pointer-heavy code
## Chunk 544 — 2026-06-12T23:55:30.803Z
- Matched: no
- Summary: Unable to decompile func_151F7F60. The target assembly requires complex struct member access at offsets 0x201C and 0x3F88, heavy pointer arithmetic and calls to bcopy/bzero. The lack of definition for the struct containing these members (and the function pointer at offset 0x04) prevented a correct C translation. Current attempts resulted in compiler errors due to re-declaration of bcopy and syntax errors in pointer math. No further progress can be made without a detailed struct layout or helper functions. Suggest obtaining struct definitions or skipping to next useful candidate.
- Likely cause / blocker: not provided
- Tooling observations: not provided
- Improvement opportunities: not provided
## Chunk 548 — 2026-06-13T00:11:16.696Z
- Matched: no
- Summary: Exhaustive decompilation attempts on func_151F7F60 failed to produce a matching C implementation; permuter search reached score 68 but did not pass full ROM SHA-1 verification. The function likely requires precise struct layout knowledge and indirect function pointer handling that is currently unavailable.
- Likely cause / blocker: Complex pointer arithmetic and indirect function call through object fields prevents accurate C reconstruction; current attempts fail to match due to missing struct context and unknown function prototype.
- Tooling observations: decomp_attempt repeatedly produced length mismatches; diff highlighted extra branches and missing stores; permuter achieved best 68 score but full ROM verification still failed; IDO context sensitivity confirmed.
- Improvement opportunities: Need correct struct layout for offsets 0x201C and 0x3F88; clarify prototype for indirect function stored at offset 0x04; provide accurate extern declarations for bcopy/bzero if used; handle indirect function pointer call with proper type.
